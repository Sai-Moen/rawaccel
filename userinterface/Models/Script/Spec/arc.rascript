Arc mode by SaiMoen

[

	Input_Offset := 0 >= 0;
	Limit := 4;
	Midpoint := 16 > 0;

]

	pLimit := Limit - 1;

{

	if (x > Input_Offset):
		x -= Input_Offset;
		y += (pLimit / x) * (x - Midpoint * atan(x / Midpoint));
	:

}